import React from 'react';
import './Toolbar.css';
import Logo from '../../Logo/Logo.js';

const toolbar = (props) => (
  <header className="Toolbar">
    <Logo />
    <div> MENU </div>
    <nav>
      ...
    </nav>
  </header>
);

export default toolbar;
