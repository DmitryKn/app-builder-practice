import React from 'react';
import './Toolbar.css';

const toolbar = (props) => (
  <header className="Toolbar">
    <div> LOGO </div>
    <div> MENU </div>
    <nav>
      ...
    </nav>
  </header>
);

export default toolbar;
